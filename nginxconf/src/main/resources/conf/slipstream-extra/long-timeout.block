    # Increase the timeout for resources that may take time to complete

    # Defined in cache.block
    #location ~ ^/run/[^/]+/?$ {
    #   proxy_pass http://slipstream_servers;
    #   include conf.d/slipstream-proxy.params;
    #   
    #   proxy_read_timeout 900;
    #}
